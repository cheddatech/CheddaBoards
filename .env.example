# =====================================================
# CheddaBoards Environment Variables
# Copy this file to .env and fill in your values
# =====================================================

# =====================================================
# CANISTER CONFIGURATION (Required)
# =====================================================

# Your deployed CheddaBoards canister ID
CANISTER_ID=xxxxx-xxxxx-xxxxx-xxxxx-cai

# ICP API host (default: https://icp-api.io)
ICP_HOST=https://icp-api.io

# Canister ID for auth-verify (usually same as CANISTER_ID)
CHEDDA_CANISTER_ID=xxxxx-xxxxx-xxxxx-xxxxx-cai

# DFX host for auth-verify (default: https://icp-api.io)
DFX_HOST=https://icp-api.io

# =====================================================
# VERIFIER IDENTITY (Required for auth-verify)
# =====================================================

# Ed25519 identity JSON for the verifier
# This principal must be registered as VERIFIER in main.mo
# Generate with: dfx identity export <identity-name>
# Format: JSON string, e.g., '["base64-public-key","base64-private-key"]'
VERIFIER_IDENTITY_JSON='["YOUR_PUBLIC_KEY","YOUR_PRIVATE_KEY"]'

# =====================================================
# OAUTH CREDENTIALS (Required for social login)
# =====================================================

# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Comma-separated list of Google Client IDs (for api.js fallback)
GOOGLE_CLIENT_IDS=client-id-1.apps.googleusercontent.com,client-id-2.apps.googleusercontent.com

# Apple Sign In credentials
# Get from: https://developer.apple.com/account/resources/identifiers
APPLE_SERVICE_ID=com.yourcompany.yourapp.web
APPLE_BUNDLE_ID=com.yourcompany.yourapp

# =====================================================
# CORS CONFIGURATION (Required for auth-verify)
# =====================================================

# Comma-separated list of allowed origins
# Include all domains that will call the auth-verify function
ALLOWED_ORIGINS=https://yourgame.com,https://anothergame.com,http://localhost:8888

# =====================================================
# OPTIONAL / DEVELOPMENT
# =====================================================

# For local development with dfx
# DFX_HOST=http://127.0.0.1:4943

# =====================================================
# NOTES
# =====================================================
# 
# 1. VERIFIER_IDENTITY_JSON:
#    - Generate a new identity: dfx identity new verifier
#    - Export it: dfx identity export verifier
#    - The principal of this identity must match VERIFIER in main.mo
#    - Keep this secret! It can create sessions for any user.
#
# 2. OAuth Setup:
#    - Google: Create OAuth 2.0 Client ID (Web application type)
#    - Apple: Create Service ID and configure Sign In with Apple
#
# 3. CORS:
#    - Add all domains where your games are hosted
#    - Include localhost for development
#    - Don't use wildcards in production
#
